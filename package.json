{
  "name": "glorious-hair",
  "version": "1.0.0",
  "private": true,
  "description": "Glorious Hair — luxury human-hair wigs storefront (static site)",
  "scripts": {
    "build": "tailwindcss -i ./assets/tailwind-input.css -o ./assets/tailwind.css --minify",
    "watch": "tailwindcss -i ./assets/tailwind-input.css -o ./assets/tailwind.css --watch",
    "dev": "npx serve ."
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
